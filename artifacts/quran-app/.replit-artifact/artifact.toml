kind = "web"
previewPath = "/"
title = "Quran Learning App"
version = "1.0.0"
id = "artifacts/quran-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18306

[services.development]
run = "bun run --cwd artifacts/quran-app dev"

[services.production]
build = [ "bun", "run", "build" ]
publicDir = "artifacts/quran-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18306"
BASE_PATH = "/"
